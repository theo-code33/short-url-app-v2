import { register, login } from "./auth";
import { createUrl, getUrl, getBaseUrl, updateUrl, deleteUrl } from "./url";
import { createApiKey, deleteApiKey } from "./api-key";

export default {
  register,
  login,
  createUrl,
  getUrl,
  getBaseUrl,
  updateUrl,
  deleteUrl,
  createApiKey,
  deleteApiKey,
};
